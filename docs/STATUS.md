v0.1.4 发布批次（2026-09-21）：按用户要求上传 GitHub 并发布网页。此前本地迭代一起进入本次版本，覆盖手动角色卡、气泡拖拽、职业水印与状态视觉、来源配置、密集规则正文、怪物图鉴和跨分类返回。下文“未发布”为各轮当时的历史记录。

发布前完整验证：31 项单元测试、38 项浏览器端到端（Windows Edge，2 workers）及生产构建通过。发布准备中发现并修复语言 / 武器词条落入通用特性框的问题，以及自定义扩展包来源名称未进入显示登记的问题；旧测试按最终手动编辑、右侧临时查阅、原始表格和当前入口同步。GitHub Actions 将在 main 推送后执行同一完整套件，成功后才发布 Pages；实际线上结果以 Actions / Pages 记录为准。

本版本原创代码、文档及美术改用 DND Card Noncommercial Share-Alike License 1.0：禁止未经授权的商用，分发 / 对外网络服务须公开完整衍生源码并沿用许可。此前 MIT 版本及第三方依赖许可保持原授权范围，详情见 LICENSING.md。上游规则快照、私人人物和临时截图不随源码上传。

# 进度

本地资料查阅交互修订（2026-09-21）：仅已启用来源内的逐条禁用显示删除线；还原原始行列表格、满宽 / 9px / 约14px行高与行悬停高亮，修复表格阻止滚轮传递。正文特性标题不再预览自身，改用自定义拖拽；列表接近底部自动追加，默认筛选对象保持稳定，避免重绘重置批次数。返回栈跨分类保存条目及阅读状态。术语清理后其他资料独立收纳；状态默认玩家手册来源，可修改并持久化。31 项单元检查与 13 项相关浏览器检查通过，覆盖禁用区别、表格滚轮与标题拖入、三级跨分类返回、连续两次加载 / 状态默认筛选，并回归独立阅读、关键词固定和左卡预览。构建通过；真实中文站表格截图无脚本异常、标题 / 表格悬停无 tooltip。未提交、推送或发布。


本地规则资料精修（2026-09-21，未发布）：冻结正文标题；中性灰白配色；职业三段定位与加宽即时目录；合并表格增强分隔及数值对齐；全局搜索、固定浮层与 tooltip 滚轮隔离；左卡悬停右侧临时查阅、移出还原、点击固定。新增武器词条 / 武器精通与按训练行拖入。职业可选特性依据数据关联展开，目录不增加独立特性分类。

来源元数据登记 184 项；对已获取上游文档中的来源标识核对，中文名称缺项为 0。规则设置提供显示方式与版本选择按钮，书籍按核心优先 / 发布时间升序排列并统计条目类型，支持逐条禁用与 DND1 配置码往返。完整资料页面载入 21,198 条（包括项目体型条目与派生版本）；怪物索引 110 文件、4,559 原始记录，跨文件复制及模板解析后未解决 `_copy` 为 0。源数据快照、临时审计脚本及测试角色仅存于被忽略的 work 目录。

追加用户图示调整：怪物使用专门的紧凑数据卡，三列属性矩阵、豁免、先攻、抗性与动作等；从来源站读取 token，标题冻结。普通正文 / 分组标题 / 属性区域不再唤起 tooltip，仅真实规则链接有预览。2024 动作标记转换为可读的中文触发、响应、豁免与攻击名称。

验证记录：31 项单元检查、9 项相关浏览器检查通过；规则资料浏览器回归覆盖来源码恢复、逐条禁用、独立阅读状态、筛选 / 折叠、冻结标题、职业选项、训练拖拽、全局搜索、左卡临时预览、嵌套 tooltip 与分隔条。怪物新增检查验证普通区域无弹窗、规则链接可预览。真实资料截图录制期间浏览器脚本错误 0，390px 窄屏 document 宽度为 390px。构建通过。没有执行完整发布套件，也未提交、推送或部署。

以下为前一轮记录：

本地魅惑与倾斜堆积微调（未发布）：魅惑保留原曲线和卷须，叶片改为爱心；从前景边缘层移至纸张背景层，位于所有框与文本之下。失能及其组合状态下，部分小框会左右倾斜，碰撞预留旋转后的范围，空间不足时减小角度；编辑模式平滑回正。

本轮构建及 1 项针对五页状态叠加的浏览器检查通过，覆盖倾斜存在、最终框体不重叠、魅惑分层、下落后输入、编辑复位及保存恢复。本轮仅本地修改，未提交、推送或部署。

本地状态视觉第二版（未发布）：死亡豁免移到熟练加值上方，状态仅以气泡列于「DND 五版角色卡」右侧并让双横线缩短；多状态可横向滚动，保留查阅、排序、整卡新增和拖出移除。死亡豁免随备份保存并校验 0～3 次；力竭层数用气泡旁 1～6 数值记录。

按用户逐条重做 18 种效果：头像倒转/透明化/椭圆星轨、禁扬声器/禁眼睛大图标、速度框全黑、框边抽动但输入不动、六段红边、间歇故障闪屏、环卡曲线藤蔓、框体重力碰撞、各框裂纹和轻灰、放大受擒手、贯穿锁链、先攻周围放射片、静态虚线、全卡红色飞溅、绿色墨水浸边、职业图标下旋转且按图形遮挡的魔法阵。职业水印退场后再切换，不同时出现两枚。用户确认的视觉继承：昏迷包含失能/倒地；麻痹、石化、震慑包含失能。全部效果覆盖五页，编辑模式恢复正常布局与外观。

验证：28 项核心检查、6 项相关浏览器检查通过（分组运行），构建通过。覆盖死亡豁免保存/校验、力竭层数恢复、状态组合和重复来源、五页碰撞无重叠、下落后控件可操作、编辑模式复位、稳定的恐慌命中框、水印最高等级与并列顺序/退场、原有技能/拖拽/体型/法术分组/快捷栏。真实中文站数据截图及两段视频在本次 outputs，录制无浏览器脚本异常。未执行完整发布套件，未提交、推送或部署。

以下为前一轮记录：

本地职业水印与状态特效（未发布）：17 种职业徽章接入中央大幅灰度水印，保持四周留白；只显示最高等级职业，并列取最前一项，等级修改/删除/刷新后同步更新。18 种状态独立响应加入与移除，允许全部同时出现，专注持续动画、石化持续静止。重复状态来源共用视觉层，切换角色不残留旧效果。背景填色透出水印，厚描边、文本与输入控件保持原有不透明度。

构建通过；4 项相关 Chromium 浏览器检查通过（分组执行，修正测试页签名称后单项重跑），覆盖水印等级与并列顺序、换页/刷新/删除、18 状态并存、独立退场/撤销、重复来源、角色切换、减少动态效果、窄屏，以及原有技能/拖拽/体型/法术分组回归。真实资料截图保存于本次 outputs 中的「DND-card-职业水印.png」「DND-card-多状态与水印.png」「DND-card-全部18种状态.png」；「DND-card-状态叠加与退出.mp4」展示全部状态同时出现，以及石化/束缚独立退场和撤销恢复，浏览器脚本异常 0。未执行完整发布套件，未提交、推送或部署。

本地视觉微调（未发布）：专精四角星取消继承的输入内边距、锁定 10 × 10 px 正方形，点击/焦点状态保持黑色；实测选中时背景 rgb(17,17,17)。法术环阶色带横向铺满内容区、移除描边及组间距，实测左右偏差与相邻段间隙均为 0。构建通过并更新截图。

前一轮设计评审稿：design/class-badges-v1 提供 17 枚自绘灰度纯色无描边 SVG 和含 24 px 样例的对照页；当时尚未接入卡面。design/STATE-EFFECTS-PROPOSAL.md 为 18 种状态的分层、入场/持续/退场方案，现已按上方记录实施。

本地卡面与资料完整性迭代（未发布）：体型可拖入，选择种族默认中型；生命骰移除资源记录入口。普通模式以空心圆/实心圆/四角星呈现无熟练/熟练/专精，编辑模式分别勾选；万事通位于敏捷技能下方，未启用时普通模式隐藏，按版本参与计算并随备份保存。状态/训练支持整卡作为新增落点，移除仍按自身框判断。束缚背景铁链有进入/退出动画且不拦截输入，空气泡不弹提示；法术按环阶归组、背景逐级加深。

资料排查补齐生成术语、物品组/模板/具体魔法装备、武器属性与精通、疾病/附加状态、角色创建选项、奖励、灵能、神祇/教团/恩惠/设施及表格；展开 additionalEntries、物品引用模板、声明式副本及种族/专长变体，修正神系与同英文表格的身份冲突。真实上游计 16,336 条，另加本地体型 12 条；本轮已加载目录的 _copy 剩余 0、下载失败 0、浏览器脚本异常 0。这不等于整站归档，具体范围见 DATA-AUDIT.md。

验证：27 项核心测试、5 项相关浏览器检查通过；真实资料截图中敏捷框 scrollHeight = clientHeight = 159，已录制束缚入场/退场视频。未执行完整发布套件，未提交、推送或部署。

等级表格本地微调（未发布）：其余列完全相同的连续等级合并为「1～3」区间，数值变化或等级断档时分段；非等级表及来源显式跨行/跨列保持原样，不改变原始资料。24 项核心检查、构建及 1 项相关浏览器回归通过；真实战士表显示 1～3、4～9、10～15、16～20，截图无脚本异常。未提交、推送或部署。

本地编辑模式与密集阅读迭代（未发布）：移除正文中的操作说明/空白说明和「加入角色卡」按钮，列表与正文标题均可拖拽。标题栏承担展开/折叠及编辑功能。Tooltip 行段间距压缩、英文同行，价格/重量改为角标。只有职业/子职提供目录；表格内容宽度靠左排布，共用结构比较合并连续相同单元格（包括真实 2024 战士回气等级段），不修改来源数据。

右上角编辑模式默认关闭；属性、熟练、训练及体型编辑受控，五个计算数值采用 sheetBonuses 独立加减而非改写结果。熟练修正传播到技能与豁免、被动察觉，升级后继续重算；旧的绝对修正备份保持兼容。新字段经过导入校验，审卡列出修正与手动体型。补充六档体型独立分类及卡面选择，按官方 2014/2024 占地和负重规则整理，明确项目作者及来源，不赋予虚构的统一身体重量。

构建、23 项核心检查通过；浏览器分组验证编辑锁/解锁、修正随等级重算与刷新恢复、标题折叠、表格合并、角标、体型分类、提示固定嵌套、气泡拖拽、多职业及资料阅读状态。真实目录 7,762 条（含本地整理的 12 条分版本体型资料）桌面/窄屏截图无脚本异常，编辑模式下全部数值框与训练栏 scrollHeight = clientHeight。旧浏览器用例入口已随移除按钮及编辑模式调整；本轮未执行完整发布套件，未提交、推送或部署。

本地气泡与资料阅读迭代（未发布）：已去掉 Tooltip 出现前的等待，鼠标跟随不再逐次触发 React 整体更新；保留右/中键固定、嵌套、外部点击消费，修正 Esc 移除遮罩后重新触发同一个提示的问题。状态和法术复用特性气泡，训练记录改成可手填/排序/拖出移除的气泡，左下状态与魅力压缩、训练增加空间，空卡和真实演示角色测得训练区 scrollHeight = clientHeight。

取消子职顶层标签，职业正文提供子职入口与拖拽；术语汇编与语言按 raw._category 分开。资料标签各自持久化正文 ID、滚动/折叠、搜索/版本、排序和多维筛选。职业/子职的等级引用默认展开，显示原始成长表，提供按等级分组的浮动目录。列表与正文压缩间距、中英文同一行；表头升降序支持数值、施法时长、距离、稀有度等语义排序。新的筛选对话框按资料字段生成现有选项，支持包含/排除、选项搜索、动态计数、单组不限与重置，不写入角色规则。

当前构建及 9 项相关浏览器检查通过（分组运行），覆盖修改后的子职入口、训练编辑和多职业恢复。新增 3 项浏览器用例验证分类间/刷新后的正文与筛选恢复、排序与排除、折叠与目录、空框无滚动、状态/法术气泡与即时提示；原有气泡排序/拖拽、固定嵌套提示和分隔线回归通过（分组运行）。真实中文站 7,750 条资料完成桌面/窄屏截图，无页面脚本异常，390 px 屏幕无横向溢出。本轮未执行完整发布套件，未提交、推送或部署。

本地手动角色卡迭代（未发布）：分隔标题为「DND 五版角色卡」；拖拽预览保持抓取点在鼠标下、最大宽 180 px。单职业上下分为职业与子职，拖入第二职业后合并，移除后恢复；身份内容居中，不设 ×，新增高光覆盖外框、标题和内容。身份和气泡拖出自身区域即移除，Esc 取消、撤销恢复，自动特性的手动移除随备份保存。

加入身份条目时，按原始资料的固定关联、当前等级及命名特性带入内容，逐项缩放出现并平滑让位；背景与专长也使用气泡。支持资料中「专长；指定选项」及冒号形式的限定引用，保留限定文字但不自动选择规则。按名称识别属性成长、技能/法术数量、选项配额和前提判定已移除，右侧不再存在填写状态或候选要求。技能/豁免自由勾选、训练手填，生命值为当前 / 上限及临时，可直接修改上限；基础计算、来源管理和显式 effects 保留。旧 answers 原文仍在备份中，属性成长改由玩家填写，旧选择不再驱动成长问答。顶部保留「修正」入口。

验证：21 项核心检查和 19 项 Chromium 浏览器检查通过（分组执行），包含自动带入/去重、来源与等级关联、手动移除不复生、抓取点偏移、取消/撤销/刷新、多职业切换、无配额熟练记录、导入/导出/存储互斥、Tooltip 固定与嵌套、分隔线和固定 A4。真实 7,750 条资料截图无脚本异常，确认侍僧授予的魔法学徒关联和多职业特性进入对应格。旧浏览器用例已改为本轮手动编辑约定；未执行生产离线项目或发布流程。未提交、推送或部署。

本地交互迭代（未发布）：收紧卡面与特性组间距；主要页职业栏仅显示名称与可编辑的 Lv. 等级，完整职业/子职操作移到「特性」详细页。资料填卡、跨页悬停与气泡排序统一采用 Pointer Events 自定义拖拽，带倾斜跟随预览、落点动画、实时让位、边缘滚动及 Esc 取消；顺序仅在有效落点松手后保存。右侧列表/正文分隔线支持拖动和键盘调整、浏览器本地记忆及窄屏边界限制。关键词和特性共用 Tooltip：鼠标右键/中键固定、嵌套查阅，点外部空白一次清空并拦截该次点击，Esc 关闭全部。

验证：构建及 8 项相关浏览器检查通过（分组运行），覆盖自定义拖拽不触发原生 dragstart、松手前排序预览、取消还原、跨来源排序与刷新、填卡和悬停切页、快捷栏、职业/子职管理、分隔线持久化与窄屏、嵌套固定提示与外部点击防误触。真实中文站 7,750 条资料加载后截图，无页面脚本异常。中键固定后松键误关的问题已修复并回归。此轮未跑完整发布流程，未提交、推送或部署；截图与演示数据留在本地。

本地特性气泡原型：数值栏居中且无实际内容溢出；身份标题加高、属性标题居中；所有旧填写文案移除，只有四类虚线格显示「点击填写」。主要页改为「特性」与「背景与专长」。特性按实际职业/种族来源分组，支持鼠标跟随提示、单项展开为粗斜体标题加正文、全栏展开/折叠、鼠标拖拽与 Alt+方向键排序；跨来源排序仍显示真实来源标题，呈现状态随角色保存，不重复授予规则效果。法师 2 级的施法/学者及精灵敏锐感官已用真实条目在隔离演示角色中验证。

本轮本地构建、18 项核心测试及 7 项相关浏览器检查通过（分组运行；数值溢出修正后气泡测试重跑通过），覆盖排序/展开刷新恢复、跟随提示、内滚动、角色填写、关键词、原有拖拽与快捷栏。仍未发布；演示角色和上游资料快照不进入 Git。

最新本地排版预览：主要页底部「待填写或核对」整栏已隐藏，角色卡内容自动占用腾出的高度，A4 尺寸保持不变；已检查更新截图，无脚本异常。该栏的工具入口后续随界面迭代重新安排。

本地 UI 预览（未发布）：已按最新六项反馈统一切角及标题裁剪、限制四类虚线外框、交换基础属性与调整值的大小/位置、重做等级与护甲铭牌，并将生命骰移到生命值下方、右侧合并头像占位框。线上仍为 v0.1.3；头像当前仅展示占位，内部词条虚线待下一轮反馈。验证与截图在本地进行，不触发 GitHub 发布。

本地构建通过；相关浏览器回归首次 4/5 通过，新裁剪层下拖拽进入落点时仅收到 dragenter、悬停状态未更新，补齐 dragenter 与 dragover 共用处理后该项单独重跑通过。桌面/窄屏真实资料截图无脚本异常。此轮未运行完整发布验证。

2026-09-21：网页首版已实现，当前 v0.1.3 在参考图布局上恢复用户原有的厚描边 StyleBox 风格。在线地址：https://fullpeople.github.io/DND-card-web/ ，源码：https://github.com/FullPeople/DND-card-web 。

本轮恢复深灰粗框、浅灰内容区与灰底白字标题带；属性和数值格为圆角，正文格为切角，缺项继续使用整格流动粗虚线。已对照 v0.1.1 截图检查桌面/窄屏及真实资料显示，无浏览器脚本异常；5 项相关浏览器回归与构建通过，涵盖 A4、页签、关键词、拖拽、缺项填写和快捷栏。发布仍须通过 GitHub 的完整 17 项核心与 14 项浏览器验证。

完成原位引导和拖拽、2014/2024、中文站目录、规则与扩展包、角色管理与恢复、撤销重做、离线工作区、审卡及枭熊文件交换。保留固定 A4 和五个页签；主要页改为顶部身份/总等级/生命资源、左侧两列属性内的技能与豁免、右侧快捷栏与特性/法术区域。子职仍合并职业，种族特性与专长合并一格，激励和状态按参考图指定位置呈现。缺项使用整格流动虚线，点击格子定位资料或选择；快捷栏支持固定、排序、查阅、取消固定和撤销。验证为 17 项核心测试、14 项浏览器端到端；GitHub Actions 执行同样的验证后部署。最后发布状态可在仓库 Actions 页面核对。

真实中文站浏览器载入 7,750 条资料；2014 与 2024 实际填卡、全量缓存断网重开、关闭浏览器进程后恢复、审卡离线及打印样式已验证。记录见 [VALIDATION.md](VALIDATION.md)，完整清单见 [ACCEPTANCE.md](ACCEPTANCE.md)。

规则覆盖不是全部自动化：复杂前提、特殊联动、部分继承和恢复规则仍需人工核对，能够留下明确修正和审卡依据。首次用户主观视觉与连续跑团反馈尚未取得。

原 Godot、音乐板、桌面骰子及枭熊项目均未修改。C++ 客户端、Excel 适配、在线账号服务留待后续，不伪装为本次已完成。
