# 2026-09-27 反馈清单：去重、修复与验收边界

> 用户复查追加：本轮起始装备适配曾引入 `blocks.map is not a function`，导致含背景数组的整份资料加载失败。已修正并用用户列出的 10 份真实文件逐份回归。原清单及图片共 33 项的白话说明、未解决项和本次证据见 [逐条更正记录](FEEDBACK-PLAIN-20260927.md)，不能把下方“本地实现”理解为所有真实问题都已解决。

本轮修改保存在隔离工作区 `F:/CodexWork/2026-09-27/feedback/{web,suite}`，分支均为 `codex/feedback-september`。Web 基于已发 `e3a9fb5`，Suite 基于 `66ca635`。两个脏主目录的历史工作未重置，也没有将其中实验整包带入。**本轮未推送、未发布；线上不会因此自动改变。**

2026-09-27 公网只读核验：Suite-dev `1.0.195-dev`，旧稳定插件 `1.3.5`，国内单机 `standalone-1.0.195-fix196`，单机 sourceCommit 为 `e3a9fb5c6732af0af0728ea7c2161f7622cdd468`。回执位于本轮隔离根目录的 `live-baseline.json`。

## 原始清单逐项处理

“本地实现”不等于已上线或真实多人已验收；详细测试见文末与分项报告。

| # | 反馈 | 本轮处理 / 已有证据 |
| --- | --- | --- |
| 1 | 主要页法术更新慢，尤其预备制 | 194 已有即时投影与远端 revision 回归。本轮保留并重新覆盖预备后立即回主要页；普通模式改为查阅，编辑模式才可改预备。 |
| 2 | 导入错误版本、禁用来源无提示 | 194 已有单卡核对。本轮扩到批量：全批严格校验后列出版本、记录等级/生效等级与禁用条目；保留记录再明确确认。另修复房间 2024 投影在 HP 编辑时永久把 2014 原卡写成 2024 的问题。 |
| 3 | Wiki 首次卡顿 | 归一化、继承及物品模板展开移入 Worker；角色变化不再重建整份 Wiki 索引。6000 条生产夹具冷启动仍有 220 ms 长任务，未宣称零卡顿。 |
| 4 | 完整导入导出、复制 JSON、PNG/PDF、任选角色 | 统一面板支持完整/枭熊兼容 JSON 文件与剪贴板、任意可见角色或集合、批量文件导入。PNG 可选五页并打 ZIP，直接生成多页 A4 PDF；可隐藏头像、背景文字、状态效果、资源或合拢特性。导出互斥、结束恢复页签/编辑模式。长正文另有完整 HTML 审卡打印，A4 图片保持原布局。 |
| 5 | 统一角色管理，批量增删与详细概况 | 角色簿增加筛选、玩家/版本/职业/HP/AC/权限摘要、多选导出、确认批量删除、按多行姓名批量创建。远端操作逐笔确认，错误停止并列出已完成数量；未知结果不自动重放。 |
| 6 | 简易/军用武器训练词条 | 两类 × 两版本，四个独立身份，可从武器词条拖入训练。 |
| 7 | DM 专门审卡界面 | 六组站内审卡：风险、属性熟练、法术资源、装备负重、条目来源、裁定依据；不伪称全规则自动合法性判定。 |
| 8 | 固定/天生/次数法术 | 独立固定/次数法术栏，不占预备位；剩余次数由 runtime 持久保存，刷新不重新领取，恢复方式为说明及显式手动操作。 |
| 9 | 导出再导入丢 Wiki 联动 | 文件/剪贴板的兼容格式附严格原生快照，保留来源、版本、条目 ID、选择与已用资源。损坏 native 不降级为自定义。保留旧格式未映射字段，只剥递归 native 副本；同名不同来源法术按选择 ID 判预备。旧时已丢身份的纯 0.3 文件不按显示名猜测重绑。 |
| 10 | 旧插件采用新卡 JSON、去 Wiki、禁 XLSX | 新旧格式统一上传桥与五页阅读器；复用 A4 样式，可查卡内快照，制卡跳网站。阅读器不请求 Wiki、拒绝 XLS/XLSX，明确只读；旧宿主的运行值入口继续独立。 |
| 11 | 怪物编辑不直观 | 常用字段、属性防御、动作分组/排序、施法、高级 JSON；复杂 AC/速度条件及未知字段保留，坏 JSON 阻止保存。 |
| 12 | 自定义分类型必填、JSON、创作提示词 | 两个创作入口共用各类字段与校验；完整 JSON 载入/复制、可复制创作提示词，嵌套和可点击标签正文往返；旧卡不会因新增创作必填项无法打开。 |
| 13 | 展示内容/骰子排版、来源副词条、Wiki 右键 | 扩展分享字段；Wiki 标题、目录、引用统一右键查看/加入/枭熊全员展示，保留来源与编辑权限。Suite 展示来源改为副行。 |
| 14 | 部分玩家没有投骰按钮 | 当前 Web 基线入口始终渲染，仅断线或禁用骰子时不可用；本轮 PLAYER 浏览器覆盖可见。历史旧包/特定真实房间差异不能凭此断言全消失，需核实玩家实际版本和房间开关。 |
| 15 | 默认头像导致打不开/无通知 | 196 已发布根因修复：回退 Token URL 不再写入 portrait；读取仅丢坏图片而保留角色。旧坏字段不自动后台回写。本轮保留该修复并定向回归。 |
| 16 | 状态的三项被强制后置 | 去掉 `_category=status` 特殊降序，跟随来源优先级与名称排序。 |
| 17 | 拼音模糊搜索、结果定位 | 中文/英文/全拼/首字母/拼音片段共用匹配；结果点击修掉提前 return，清目标类别旧过滤，滚动到虚拟列表对应行。 |
| 18 | 普通模式不该显示拖拽光标 | 身份和特性按实际权限显示默认光标；同时封住键盘删除/排序、中键预备绕过。状态和物品保留既有允许操作。 |
| 19 | 编辑开关移到 A4 右侧且全局 | 角色与怪物工具栏统一位置；切卡、保存怪物及刷新保留使用者偏好，实际写权限仍独立约束。 |
| 20 | 枭熊切卡/总览卡顿、缓存增量 | 找到 3 秒缓存 TTL 与 4 秒轮询必然整卡 GET、纯位置事件重建目录、重复序列化大文档等根因；改事件失效、相关字段筛选、低频兜底与目录缓存。30 次纯移动，旧版 210 次完整目录重建，本轮 0。独立复查补怪物覆盖资料刷新和最新棋子引用；9 项含权限撤销回归通过。热切卡旧版已有缓存，不能把其原本的快当成本轮提升。并非所有事件都已改成字段级传输。 |
| 21 | 旧公告专业化、新插件截图 | 去掉旧金色渐变和多余盒框，采用简洁布局并使用本轮原创夹具的真实工作台截图；见 Suite 分项证据。 |
| 22 | 图鉴放怪间歇失败，两版都修 | 共享放怪路径处理图片超时、重试及占位，不缓存失败；资料保存失败不再继续生成坏绑定 Token。受影响功能两版共用同一源码。 |
| 23 | 自动归属已共享仍不可见 | 补无绑定角色/坐骑及怪物的自动队伍光源路径；显式个人归属、DM-only 与隐藏限制仍保留。 |
| 24 | 枭熊更新公告及 Owner 指南 | 枭熊与单机分开记忆已确认版本；首次/更新弹出，Esc/遮罩不关闭，仅“我知道了”确认；顶栏可重开。移入两步 Owner 说明与三张原图，含独立网站入口。 |

## 截图补充反馈

| 补充项 | 处理 |
| --- | --- |
| 生命值 `+ - * /` 与直接扣血算式 | 有限语法解析器，支持括号，禁止执行代码；`-5` 扣 5，`=20` 设 20。枭熊排队时按最新权威基值计算，保留冲突保护。 |
| 普通法术点击查阅、编辑才能预备 | 同 #1，覆盖点击、右键、拖拽与中键。 |
| 无角色卡/怪物卡时 HP 气泡不显示 | 无数据的显式 HP 条显示待设置占位，不杜撰 HP；首次填写后正常显示。 |
| 玩家可见不可开的门 | 新增仅 DM 开关的可见门类型，显示与交互权限分开。 |
| 三龙牌更新后卡顿 | 修空闲手牌特效每帧重复阴影重绘；性能结论仍限具体热点，不是整局真实设备验收。 |
| 骰子历史显示当次公式 | 广播与两处历史都保留原始公式，包括 max/min 等表达式。 |
| 2024 职业初始装备和熟练不显示 | 补 `startingProficiencies` 和 `startingEquipment.entries/defaultData` 显示，不按职业名硬编码，也不自动发放。 |
| DM 不入座可全能；创建者离开移交 | 修旁观全能的 UNKNOWN_SEAT 和获准远端 DM 门控；主动离开采用加密私有存档、继任者校验并保存、ACK 后才切主持身份，保留局中座位、手牌与待处理队列。最终证据见 Suite 分项；突然崩溃/掉线不等同于主动移交。 |

## 本轮验证

全部是本地定向检查，未执行整个历史发布门禁，未部署。

- Web 管理/导出/公告/原生版本：`playwright.feedback.config.ts` 4 项浏览器检查；JSON 集合、全批坏文件拒绝、身份往返、同名法术、删除保留、ZIP CRC 与 PDF 对象偏移等核心检查。下载的 ZIP 用 Python zipfile 核验，PDF 用 PyMuPDF 确认五张 A4 并渲染检查。
- Wiki 与法术：22 项核心、4 项生产浏览器检查、生产 Worker 构建，详见 [Wiki 分项](FEEDBACK-WIKI-20260927.md)。
- 审卡、怪物编辑、算式、旧阅读器：9 项核心与4 项浏览器检查，详见 [界面分项](FEEDBACK-REVIEW-20260927.md)。
- Suite：以真实 SDK 跨 iframe 夹具、HTTP 模拟权威、纯逻辑与源码 A/B 性能探针验证；命令和最终数量见 [Suite 分项](FEEDBACK-SUITE-20260927.md)。真实房间未冒充已测。
- 新增 `pinyin-pro@3.29.4` 固定依赖。没有新建自动规则配额，没有发布 Buff 实验、C++ 或任何原生参考工程。

截图和已下载的五页 PDF/PNG 位于 `F:/CodexWork/2026-09-27/feedback/artifacts`；其中 `README.md` 为目录，`verification.json` 保存尺寸、页数与 SHA-256。旧阅读器另补生产嵌套验证：修正条件 lazy 导致阅读器 CSS 未被载入的问题，上传、刷新及窄面板显示均经过生产 bundle 检查。

## 后续发布前的明确边界

1. 当前主目录仍为历史混合工作区；继续使用本轮隔离源码，不能从主目录直接整包发布。Web 修改必须与本轮 Suite 源码配对构建。
2. 真实多人、两设备、低性能机器、全量 Wiki 冷加载、稳定插件上传服务完整联调尚未验收。首次 Wiki 仍有短长任务，不能宣传无卡顿。
3. 旧阅读器是只读卡，不会自动获得工作台的所有写入能力。旧文件已经缺失的唯一条目身份不能凭名字恢复。
4. 全套发布验证时，旧用例的“普通模式可改预备”和“切卡关闭编辑”预期须跟随本轮用户决定；不得为通过旧断言撤回新行为。
5. 发布/推送须等用户要求统一验证和发布；版本号与公告中的已修/遗留清单在正式发布时统一更新，当前未改变线上状态。
