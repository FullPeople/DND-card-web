# 2026-09-21 网页验收记录

## 自动化

- 16 项核心测试：来源与版本身份、关闭规则不删除内容、旧版兼容、属性与技能选择、父种族继承、子职引用、职业法术列表、前置变化后暂停效果、包版本与依赖冲突、危险导入拒绝、原生与枭熊交换、审卡转义、生命值与效果语义。
- 9 项真实浏览器端到端：原位填写和鼠标拖拽、撤销/重做、刷新持久化、角色/版本隔离、自定义包的安装和关闭、导出/重新导入、人工修正和资源、缓存更新失败与损坏保护、窄屏和键盘、多标签页写入互斥，以及生产构建断网重开。
- 本地为 Windows Edge；GitHub CI 为 Linux Chromium。工作流先验证再发布，不绕过失败部署。

## 真实中文站

2026-09-21 从 `5e.kiwee.top` 在实际浏览器中载入 **7,750 条**资料，无读取失败。计数是当天上游快照，不是长期固定承诺。

2024：创建法师、填写背景和种族，三个一级职业特性各匹配到唯一正确条目；戏法候选来自中文站职业法术索引。填写后导出、刷新，角色和资料来源保留；没有浏览器脚本异常。

2014：创建法师、高等精灵；两个一级职业特性各匹配到唯一条目。父种族与亚种族叠加后，基础 10 的敏捷变为 12，智力变为 11。升级到 2 级得到 8 个对应子职候选，无脚本异常。

生产构建：联网载入后将浏览器设为离线并刷新，角色名与全部 7,750 条资料恢复，显示 49 份缓存。另用独立持久化浏览器配置，关闭浏览器进程再启动，验证角色名和选择恢复。

## 视觉与交互

按原场景 `dnd_5e_card.tscn`、`number_item.tscn`、`box_item.tscn` 的结构和 StyleBox 参数重建：纸张、左侧六属性、两列详细区、底部标题、浅灰内容、深灰外框；普通框用切角，数字框保留圆角。对照源码参数检查实际浏览器截图，不宣称逐像素等同 Godot 渲染。

检查桌面 1512×982、窄屏 390×844、规则弹窗、真实词条、角色滚动与离线审卡。DM 审卡在离线 HTML 中打开，并检查打印样式，无横向溢出。窄屏改为卡片/资料切换，角色纸张可横向滚动和缩放。

## 验收中修复的具体问题

- 最后一项选择完成时控件马上消失：改为保留选中的技能与属性选择，避免焦点丢失。
- 原枭熊字段映射不正确：按现有 schema 0.3 改为 `core_stats`、技能数组、分组特性、`spellcasting` 和 `inventory`。
- 只读取亚种族导致丢失父种族属性：补入可确定的继承；复杂继承明确提示人工复核。
- 升级/降级或删除上级条目后，旧选项仍可能提供效果：保留记录并暂停不再符合要求的效果。
- 离线网页有缓存仍无法加载脚本：Vite 静态响应含 `Vary: Origin`，预缓存与模块请求不同导致未命中；只对已知、固定构建资源忽略 Vary 匹配。已加入生产离线回归。
- 主编辑页关闭后只读页有时仍保留只读状态：等待浏览器释放写锁后重新读取最新文档并自动接手。

## 实际边界

完整网页工作区已经实现，规则自动化范围见 [覆盖表](RULE-COVERAGE.md)。复杂前提、资源恢复、特殊特性联动和部分继承仍由玩家/DM 核对，并支持明确人工修正。

枭熊 JSON 的结构与本地实际插件格式对照并经过往返测试；没有声称已完成线上绑定或生产房间同步。C++ 客户端、Excel 转换、账号服务与付费绑定不在本次网页阶段。

还没有用户本人对视觉偏好和长时间跑团使用作出的验收；这部分不能用自动化结果代替。
