# Full Suite-dev 1.0.175 · 法术、头像、资料与场内播报

- 公共仓库可折叠，并在本机记住各仓库的折叠状态。总览资源格从约 62px 缩至 30px，资源名 11px → 5.5px、标题行 18px → 9px；角色名 14px → 7px。资源锁与设置在右上并排，悬停、键盘聚焦或触屏时显示。纸卡快捷栏资源使用紧密直角底，次数圆点从 7px 增至 14px。
- 子职业入口换行显示完整名称，来源另行使用更小字号。单行身份框按实际宽度缩小标题，不改动保存的名称。
- 全局搜索改为全界面唯一状态，保留独立结果层，不参与分类条目筛选、不重置分类滚动。旧版只迁移当前分类的搜索词，停止保存多份分类搜索词。
- 头像修复详细页的绝对定位和跨框尺寸的位置计算。背景页使用独立 `illustration`，标题为「立绘」，默认空白。头像、立绘各自压缩至最长边 1280、图片不超过 450 KB，以 data URL 随角色保存；独立版存 IndexedDB，连接版随 `dnd_card_web` 文档同步。UI 基准色与纸面隔离，原灰色纸卡及角色自己的色盘保持独立。
- 新增施法职业后立即初始化法术设置和法术位；特性同步和资源同步都执行，避免逻辑短路漏算。子职业等级完全取对应主职业；特性归属使用主职、子职名称与来源身份，正文嵌套条目携带归属上下文。
- 预备法术制显示空位网格；已学与预备法术均使用物品式格子。普通模式可点击准备或取消，实际格子飞向对应位置。记录保留已学法术与预备引用分离，撤销、保存和刷新沿用角色事务。
- 场内通知从旧资源模块生命周期中拆出，在工作台启动时立即注册；本机直接入队、远端广播，避免依赖 SDK 自回环。资源、公共拿放、自身背包、预备/取消预备均在成功持久化后播报。失败保存和无关保存不播报，锁定隐私与 DM 静默继续生效。提示层重建通过通知身份去重，保留原样式与提示音。

## 验证边界

通知专测使用实际工作台入口和提示页，只模拟 Owlbear SDK 边界；覆盖旧模块阻塞、资源模块关闭、本机无回环、通知重建、隐私及失败保存。真实枭熊房间和实体平板未实测。

首轮组合浏览器检查因 D 盘临时输出空间不足在截图/trace 写入时失败；不将该轮计为通过。改用工作区内独立临时目录后重新验证，最终结果记在 STATUS.md。

最终本地结果：64 项单元、10 项网页浏览器检查、53 项工作台联动与 19 项独立通知检查通过；库存事务、8 组合并和 9 组库存同步通过。通知专测包含 11 次宿主持久化；联动包含 28 次保存。真实房间与实体平板仍未验收。

已部署 1.0.175-dev：服务器 595 文件、公网 235 文件及源码包匹配；stable 335 文件未变，版本 1.3.5。回滚目录 /var/www/obr-plugins/suite-dev-before-workbench-1.0.175，中继 /opt/obr-workbench-relay-dev/before-1.0.175。公网收据见 Suite workbench-test-output/public-1.0.175-dev.json。本地预览 http://127.0.0.1:5197/preview 使用模拟房间。
