# 发布 185：规则资料与统一信息记录

本次范围：法术攻击 / DC 的独立手动调整格只在编辑模式显示，默认为 0；主要页始终显示戏法；气泡 / 法术 / 物品菜单支持向全员展示；枭熊信息通知进入骰子记录区，支持 Wiki 跳转及倒计时。未开启扩展从列表、搜索和正文隐藏，已开启扩展中单独禁用的条目保留删除线。

按用户最新要求撤回本轮屏幕适配。PaperFrame、responsive177.css、Workbench.tsx 与 184 发布版相同，保留此前已有的窄屏行为。不发布新增 Buff；保留原有状态效果。单机构建继续排除枭熊 SDK 与联机同步逻辑。

发布候选由 184 独立快照组装，未重置主工作目录。验证与正式上线结果见 STATUS.md；真实多人房间不以模拟测试代替。
